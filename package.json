{
  "name": "diffdock",
  "version": "0.1.0",
  "description": "A local text diff tool for browser and Electron.",
  "main": "main.js",
  "private": true,
  "productName": "DiffDock",
  "scripts": {
    "start": "electron .",
    "build:mac": "node scripts/build-mac-app.js",
    "check": "node --check main.js && node --check preload.js && node --check diff-engine.js && node --check app.js && node --check scripts/build-mac-app.js"
  },
  "devDependencies": {
    "electron": "^41.2.1"
  }
}
