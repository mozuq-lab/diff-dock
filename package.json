{
  "name": "diffdock",
  "version": "0.1.0",
  "description": "A local text diff tool for browser and Electron.",
  "main": "main.js",
  "private": true,
  "productName": "DiffDock",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check diff-engine.js && node --check app.js"
  },
  "devDependencies": {
    "electron": "^41.2.1"
  }
}
